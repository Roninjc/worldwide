# Dependencies — installed fresh via `npm ci` inside the image
node_modules
**/node_modules

# Version control & CI
.git
.gitignore
.github

# Build outputs — regenerated by `npm run build`
build
.svelte-kit

# The encrypted sync relay is a separate Cloudflare Worker, not part of the PWA image
relay

# Editor / OS cruft
.vscode
.DS_Store
**/.DS_Store

# Docs (not needed to build the app)
*.md
